site_name: pytest-impacted
site_description: A pytest plugin that selectively runs tests impacted by codechanges via git introspection, ASL parsing, and dependency graph analysis.
site_author: PromptRomp

theme: readthedocs

repo_url: https://github.com/promptromp/pytest-impacted

pages:
- Home: index.md
